import React from "react";
import styled from "@emotion/styled";

const RequestContainer = styled.div``;

function Container({ children }) {
  return <RequestContainer>{children}</RequestContainer>;
}

export default Container;
